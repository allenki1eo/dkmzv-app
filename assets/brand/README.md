Runtime copies of files from `/brand` (source of truth).

See `docs/BRAND_ASSETS.md` for drop paths.

Official KKKT DKMZV emblem. Adaptive launcher background is solid `#FFFFFF`.
The splash field is near-black `#0F1419`.
